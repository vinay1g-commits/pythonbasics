#tuple is similar to list(ordered collection of any data types) but
# unlike list tuples are immutable means once created you cannot change

user_info = ("admin","pass")
print(user_info[0])

"""
Tuples are useful when you want to store data that should not be modified 
(e.g., configuration settings).
"""